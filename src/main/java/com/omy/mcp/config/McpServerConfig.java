package com.omy.mcp.config;

import org.springframework.context.annotation.Configuration;

/**
 * MCP服务器配置类
 * 配置已移到 application.yml 中，此类保留用于将来可能的扩展配置
 */
@Configuration
public class McpServerConfig {
    // MCP 服务器配置已在 application.yml 中通过 spring.ai.mcp.server.* 属性配置
    // 无需额外的 Java 配置
}
